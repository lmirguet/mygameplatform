# Environment variables for local/dev/prod compose setups.
# Values will be defined as implementation proceeds (auth/jwt, db, hostnames, rate limits).

# auth-service
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mygameplatform?sslmode=disable
JWT_SIGNING_SECRET=change-me
JWT_TTL_SECONDS=3600
PORT=8080
WEB_DIST_DIR=web/dist
